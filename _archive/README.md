# Archived site

The previous logansinclair.me (Instrument Serif + Geist, dark, single page),
retired 2026-09-10 when the current site replaced it at the repo root.

Kept for reference. This folder starts with an underscore, so GitHub Pages'
Jekyll build leaves it out of the published site. To view it, open
`index.html` here directly; it links to `../resume.pdf`.
